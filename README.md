# Deck

Agent UI prototype — a command center for managing AI agent conversations across projects.

## Features

- **Project sidebar** with conversation status indicators
- **Playbook** (agent config) folder tree with change indicators
- **Skills section** with hover descriptions and global/repo grouping
- **Multi-conversation interface** with working/waiting/done states
- **Terminal-like input** with model selector

## Stack

- SvelteKit 5 with runes (`$state`, `$derived`)
- Tailwind CSS v4
- Dark zinc theme with violet accents

## Development

```bash
npm install
npm run dev
```

## License

MIT
